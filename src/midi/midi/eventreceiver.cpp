#include "midi/midi.h"
#include <iostream>

/*void note_on(Duration dt, Channel channel, NoteNumber note, uint8_t velocity) = 0; */
/*void note_off(Duration dt, Channel channel, NoteNumber note, uint8_t velocity) = 0; */
/*void polyphonic_key_pressure(Duration dt, Channel channel, NoteNumber note, uint8_t pressure) = 0; */
/*void control_change(Duration dt, Channel channel, uint8_t controller, uint8_t value) = 0; */
/*void program_change(Duration dt, Channel channel, Instrument program) = 0; */
/*void channel_pressure(Duration dt, Channel channel, uint8_t pressure) = 0; */
/*void pitch_wheel_change(Duration dt, Channel channel, uint16_t value) = 0; */

/* void midi::EventReceiver::meta(Duration dt, uint8_t type, std::unique_ptr<uint8_t[]> data, uint64_t data_size) */
/* { */
/*     std::cout << "Received meta event with type: " << type; */
/*     std::cout << "and duration: " << dt << "\n"; */
/* } */

/* void sysex(Duration dt, std::unique_ptr<uint8_t[]> data, uint64_t data_size) = 0; */
